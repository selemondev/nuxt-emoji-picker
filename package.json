{
  "name": "nuxt-emoji-picker",
  "version": "1.2.3",
  "description": "Simple and Powerful Emoji Picker for Nuxt 3 ✨",
  "packageManager": "pnpm@10.30.1",
  "repository": "selemondev/nuxt-emoji-picker",
  "author": "Selemondev",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/module.mjs"
    }
  },
  "main": "./dist/module.mjs",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepack": "nuxt-module-build build",
    "dev": "nuxi dev playground",
    "dev:build": "nuxi build playground",
    "dev:prepare": "nuxt-module-build build --stub && nuxt-module-build prepare && nuxi prepare playground",
    "release": "npm run lint && npm run prepack && changelogen --release --hide-author-email && npm publish && git push --follow-tags",
    "lint": "eslint .",
    "test:types": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit"
  },
  "dependencies": {
    "@nuxt/kit": "^4.3.1",
    "vue3-emoji-picker": "^1.1.8"
  },
  "devDependencies": {
    "@nuxt/devtools": "^2.7.0",
    "@nuxt/eslint-config": "^1.15.1",
    "@nuxt/module-builder": "^1.0.2",
    "@nuxt/schema": "^4.3.1",
    "@types/node": "^25.3.0",
    "changelogen": "^0.6.2",
    "eslint": "^9.39.2",
    "nuxt": "^4.3.1",
    "typescript": "latest",
    "vue-tsc": "^2.2.12"
  }
}
